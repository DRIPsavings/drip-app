name: drip
description: "DRIP — Real Drink Specials Near You."
publish_to: 'none'

# Version: Increase this every time you want a fresh build
version: 1.0.9+10

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.1.0
  share_plus: ^10.0.0
  google_maps_flutter: ^2.12.3
  url_launcher: ^6.3.0
  speech_to_text: ^7.3.0
  geolocator: ^13.0.0
  http: ^1.2.2
  shared_preferences: ^2.3.0

flutter:
  uses-material-design: true

  assets:
    # Splash screen
    - assets/splash.png
    
    # Greeting voice files (must match your folder structure)
    - assets/greetings/
    
    # Home screen buttons
    - assets/smoothie_button.png
    - assets/coffee_button.png
    - assets/party_button.png
    - assets/instant_alerts.png
    - assets/selfie_share.jpg
    - assets/family_mode_button.png